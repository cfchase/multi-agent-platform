# Multi-Agent Platform - Cluster (Dev) Configuration
# Copy to .env and fill in your values.
# Run: make config-setup-cluster  (then: make config-generate to create deployment artifacts)
#
# Auto-generated values (secrets, internal URLs) go directly into deployment
# artifacts (k8s overlays, Helm secrets). This file is NEVER mutated by scripts.

# ========================================
# OAuth (REQUIRED for cluster deployment)
# ========================================
# Google OAuth Setup:
#   1. Go to https://console.cloud.google.com/apis/credentials
#   2. Create OAuth 2.0 Client ID (Web application)
#   3. Add redirect URI: https://<app-route>/oauth2/callback
#   4. Copy Client ID and Client Secret below
# See docs/AUTHENTICATION.md for detailed walkthrough
OAUTH_CLIENT_ID=your-client-id
OAUTH_CLIENT_SECRET=your-client-secret
# OAUTH_ISSUER_URL=         # Set for OIDC providers (Keycloak, Okta)

# ========================================
# LLM API Keys (set at least one)
# ========================================
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
# GEMINI_API_KEY=

# ========================================
# Google Drive Integration
# ========================================
# App-level OAuth client credentials (NOT user-specific).
# Needed by LangFlow to refresh user Google Drive tokens during flow execution.
# Uses a DIFFERENT redirect URI than the OAuth2 Proxy login.
# Add redirect URI: https://<app-route>/api/v1/integrations/oauth/callback/google_drive
# Can use the same client ID as OAuth above if both redirect URIs are added.
# GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
# GOOGLE_CLIENT_SECRET=your-google-client-secret

# ========================================
# Langfuse Admin User (optional)
# ========================================
# Set to real email/name for the Langfuse web UI admin login.
# Password is auto-generated if not set. Also used for Langflow superuser.
# LANGFUSE_INIT_USER_EMAIL=admin@your-domain.com
# LANGFUSE_INIT_USER_NAME=Admin
# LANGFUSE_INIT_USER_PASSWORD=

# ========================================
# Google Cloud / Vertex AI (REQUIRED for Gemini + Claude)
# ========================================
# Used by LiteLLM for Vertex AI model access (Gemini, Claude on Vertex).
# Auth: Vertex AI uses Application Default Credentials (ADC).
#   Option A: GCP service account key JSON (mount as K8s secret, set path below)
#   Option B: Gemini API key from https://aistudio.google.com/apikey (set GEMINI_API_KEY above)
#             NOTE: Option B requires code change to use gemini/ prefix instead of vertex_ai/
# GOOGLE_CLOUD_PROJECT=your-gcp-project-id
# GOOGLE_CLOUD_LOCATION=          # Default: "global" for Gemini, "us-east5" for Claude (auto-detected)
# GOOGLE_APPLICATION_CREDENTIALS=  # Path to mounted service account key JSON

# ========================================
# Granite Guardian (Safety Validator)
# ========================================
# Required for enterprise agent safety validation.
# NOTE: Internal Red Hat endpoints (apps.int.stc.ai.prod) are NOT reachable
# from external clusters. Need a publicly accessible endpoint or VPN.
# GRANITE_GUARDIAN_ENDPOINT=https://your-granite-guardian-endpoint/v1
# GRANITE_GUARDIAN_API_KEY=your-granite-api-key
# GRANITE_GUARDIAN_MODEL_NAME=ibm-granite/granite-guardian-3.3-8b

# ========================================
# Granite 4 (Document Synthesis Processing)
# ========================================
# Used for pre-processing documents into Markdown for faster querying.
# Uses OpenAI-compatible API (hosted_vllm provider in LiteLLM).
# GRANITE4_ENDPOINT=https://your-granite4-endpoint/v1
# GRANITE4_API_KEY=your-granite4-api-key
# GRANITE4_MODEL_NAME=ibm-granite/granite-4.0-tiny-preview

# ========================================
# Langflow Configuration (optional)
# ========================================
LANGFLOW_DEFAULT_FLOW=enterprise-agent-multiuser

# ========================================
# Database Password (optional - auto-generated if not set)
# ========================================
# Set this only if you need a specific password.
# If left empty or "changethis", generate-config.sh auto-generates one.
# POSTGRES_PASSWORD=

# ========================================
# Web Search APIs (optional)
# ========================================
# TAVILY_API_KEY=
# GOOGLE_API_KEY=
# GOOGLE_CSE_ID=
