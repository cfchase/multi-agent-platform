# Frontend Configuration (Cluster/Dev)
# Only VITE_ prefixed variables are exposed to the browser.
# In production, nginx proxy handles API routing.
#
# Legend:
#   Uncommented = REQUIRED
#   Commented   = OPTIONAL

# --- REQUIRED ---

# Empty so relative /api/ paths are used via nginx proxy
VITE_API_URL=
