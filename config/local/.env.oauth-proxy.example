# OAuth2 Proxy Configuration (Local)
# Required for ENVIRONMENT=development in backend.
# When no credentials are set, a mock OAuth server is used.
#
# Legend:
#   Uncommented = REQUIRED (must be set for real OAuth)
#   Commented   = OPTIONAL

# --- REQUIRED (for real OAuth — leave commented for mock server) ---

# OAUTH_CLIENT_ID=your-client-id
# OAUTH_CLIENT_SECRET=your-client-secret
OAUTH_COOKIE_SECRET=changethis_generate_a_secure_random_key

# --- OPTIONAL ---

# Provider (leave empty for Google default, or set OIDC issuer URL)
# OAUTH_ISSUER_URL=https://your-keycloak/realms/your-realm
