apiVersion: v1
kind: Service
metadata:
  name: langflow
  labels:
    app: langflow
spec:
  selector:
    app: langflow
  ports:
  - name: http
    port: 7860
    targetPort: 7860
    protocol: TCP
  type: ClusterIP
