apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: langflow-route
  labels:
    app: langflow
spec:
  to:
    kind: Service
    name: langflow
    weight: 100
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
