apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# MLFlow - Development Environment
# Deploy with: kubectl apply -k k8s/mlflow/overlays/dev
# Requires: postgres-secret must exist

resources:
  - ../../base

namespace: multi-agent-platform-dev

labels:
  - pairs:
      environment: development
