apiVersion: v1
kind: Service
metadata:
  name: multi-agent-platform
  labels:
    app: multi-agent-platform
spec:
  selector:
    app: multi-agent-platform
  ports:
  - name: public
    port: 4180
    targetPort: 4180
  type: ClusterIP
