# OAuth2-Proxy Secrets - Copy to oauth-proxy-secret.env and fill in values
# DO NOT commit oauth-proxy-secret.env to git!

# Generate cookie secret with: python -c "import secrets; print(secrets.token_urlsafe(32))"
cookie-secret=generate-a-random-32-byte-base64-string

# ===================
# Provider: Google OAuth (recommended)
# ===================
# 1. Go to https://console.cloud.google.com/apis/credentials
# 2. Create OAuth 2.0 Client ID (Web application)
# 3. Add authorized JavaScript origin:
#    https://multi-agent-platform-multi-agent-platform-dev.apps.<cluster-domain>
# 4. Add authorized redirect URI:
#    https://multi-agent-platform-multi-agent-platform-dev.apps.<cluster-domain>/oauth2/callback
# 5. Also set OAUTH2_PROXY_PROVIDER=google in oauth-proxy.env
client-id=your-google-client-id.apps.googleusercontent.com
client-secret=your-google-client-secret

# ===================
# Provider: GitHub OAuth (alternative)
# ===================
# 1. Go to https://github.com/settings/developers
# 2. Create new OAuth App
# 3. Set callback URL: https://<your-route>/oauth2/callback
# 4. Also set OAUTH2_PROXY_PROVIDER=github in oauth-proxy.env
# client-id=your-github-client-id
# client-secret=your-github-client-secret
