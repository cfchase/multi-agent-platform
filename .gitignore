# Claude Code
.claude/settings.local.json

# Root npm dependencies (for concurrently)
node_modules/
package-lock.json

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Centralized config (keep .example files, ignore actual configs)
config/local/.env
config/local/.env.*
!config/local/.env.example
config/local/flow-sources.yaml
config/dev/.env
config/dev/.env.*
!config/dev/.env.example
config/dev/flow-sources.yaml

# IDE and Editors
.vscode/
.idea/
*.swp
*.swo
*.sublime-project
*.sublime-workspace
.vim/

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
Desktop.ini

# Docker
.docker/

# Logs
logs/
*.log

# Temporary folders
tmp/
temp/
.tmp/

# Kubernetes/OpenShift secrets (if any get created locally)
*.key
*.crt
*.pem
secrets/

# K8s secrets (keep .example files, ignore actual secrets)
k8s/**/oauth-proxy-secret.env
k8s/**/langflow-secret.env
k8s/**/*-secret.env
k8s/**/backend-config.env
k8s/**/allowed-emails.txt
!k8s/**/*-secret.env.example

# Config files with personal info (keep .example files)
config/dev/allowed-emails.txt
config/dev/namespace-admins.txt

# Helm secrets (keep .example files, ignore actual secrets)
helm/*/secrets-*.yaml
!helm/*/secrets-*.yaml.example

# Local development overrides
.local/

# Backup files
*.bak
*.backup
*~

# Playwright MCP
.playwright-mcp/
.planning/
app.db
